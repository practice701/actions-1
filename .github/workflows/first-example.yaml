name: my first workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
        
      - name: Welcome message
        run: echo "My first workflow - testing"

      - name: List file
        run: ls

      - name: Read file
        run: cat README.md